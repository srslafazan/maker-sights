# Maker Sights

Dependencies

- Docker client
- Docker compose
- Node + npm

Setup

- Install node_modules in ./gateway and ./angular

Build
```bash
docker-compose build
```

Start
```bash
docker-compose up
```
